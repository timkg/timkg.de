
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "install node and npm"'
                sh 'echo "Jenkins triggered on PR merged"'
            }
        }
    }
}
